{
  "name": "helper-utility",
  "version": "0.0.0-development",
  "description": "helper utility",
  "main": "./lib/index.js",
  "repository": {
    "type": "git",
    "url": "git@github.com:jameswlane/helper-utility.git"
  },
  "keywords": [
    "utility"
  ],
  "bugs": {
    "url": "https://github.com/jameswlane/helper-utility/issues"
  },
  "homepage": "https://github.com/jameswlane/helper-utility#readme",
  "scripts": {
    "audit": "npm audit",
    "audit:fix": "npm audit fix",
    "clean": "rimraf lib coverage",
    "commit": "git-cz",
    "commitmsg": "opt --in commit-msg --exec \"validate-commit-msg\"",
    "contributors:add": "all-contributors add",
    "contributors:generate": "all-contributors generate",
    "coverage:report": "coveralls",
    "precommit": "lint-staged && opt --in pre-commit --exec \"npm run verify\"",
    "semantic-release": "semantic-release",
    "setup": "npm i -g npmvet david depcheck types-checker commitizen semantic-release-cli all-contributors-cli",
    "test": "jest --coverage",
    "test:ci": "jest --maxWorkers=4",
    "test:coverage:ci": "jest --coverage --maxWorkers=4",
    "test:coverage:codecov": "codecov",
    "test:watch": "jest --watch",
    "ts:build": "tsc",
    "ts:watch": "tsc -w",
    "tslint": "tslint --config tslint.json --project tsconfig.json \"src/**/*.ts\"",
    "tslint:fix": "tslint --config tslint.json --project tsconfig.json --fix \"src/**/*.ts\"",
    "verify": "npm run verify:npmvet && npm run verify:david && npm run audit:fix && npm run verify:tscheck && npm run tslint && npm run test",
    "verify:offline": "npm run verify:npmvet && npm run tslint && npm run test",
    "verify:ci": "npm run verify:npmvet && npm run audit:fix && npm run tslint && npm run test:ci",
    "verify:check": "depcheck",
    "verify:david": "david",
    "verify:npmvet": "npmvet --renderer ci --strict",
    "verify:tscheck": "types-checker"
  },
  "files": [
    "lib/"
  ],
  "author": "James W. Lane",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "10.12.18",
    "all-contributors-cli": "5.10.2",
    "awesome-typescript-loader": "5.2.1",
    "codecov": "3.1.0",
    "commitizen": "3.0.5",
    "coveralls": "3.0.2",
    "cz-conventional-changelog": "2.1.0",
    "jest": "24.0.0",
    "lint-staged": "8.1.0",
    "opt-cli": "1.6.0",
    "rimraf": "2.6.3",
    "semantic-release": "15.13.3",
    "ts-jest": "23.10.5",
    "tslint": "5.12.1",
    "tslint-config-airbnb": "5.11.1",
    "typescript": "3.2.4",
    "validate-commit-msg": "2.14.0"
  },
  "config": {
    "commitizen": {
      "path": "./node_modules/cz-conventional-changelog"
    }
  }
}
